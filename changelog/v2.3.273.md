# v2.3.273

## Bug Fixes

- **bot-ws**: WS 断连后自动 Fallback 到 Agent API，确保耗时任务/重启时回复不丢失
  - 新增 `peerId`/`peerKind` 到 `ReplyContext`，支持断连时的备用路由
  - `deliver()` 和 `fail()` 方法在 WS 断开时自动切换到 `sendAgentApiText()` 发送
  - 覆盖场景：Gateway 重启、WS 断线重连、长时间任务卡住后恢复

## Improvements

- **markdown**: 恢复表格和代码块的 markdown_v2 渲染支持（从 main 分支合并）
  - 表格不再强制转为纯文本，保留 markdown 表格格式
  - 代码块保留原始格式显示

## Chores

- 包名从 `@yanhaidao/wecom` 更名为 `@huo15/wecom`
